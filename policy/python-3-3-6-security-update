// Update to latest Python and related modules for security fixes

on job::/ {
    { package.retire "package::/apcera/pkg/runtimes::python-3.3.5" }
}

on job::/ {
    { package.retire "package::/apcera/pkg/runtimes::python-3.3.3" }
}
